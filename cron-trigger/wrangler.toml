name = "stock-cron-trigger"
main = "worker.js"
compatibility_date = "2024-01-01"

[triggers]
# Cloudflare cron: 用名稱避免數字歧義（CF 的數字解讀與標準 cron 不同）
crons = ["30 7 * * MON-FRI"]

[observability]
enabled = true
